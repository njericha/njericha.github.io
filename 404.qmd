---
title: "404"
---

The page you requested cannot be found (perhaps it was unplugged).

Let

$$
f(x) = \underbrace{((x^\sqrt{x} + x)^\sqrt{x} \dots + x)^\sqrt{x} + x}_\text{$\sqrt{x}$ times}.
$$

Then,
$$
f(4) = (4^\sqrt{4}+4)^\sqrt{4}+4 = 404.
$$
